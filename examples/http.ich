use core/io
use core/basehttp

let handle req res =
    let _ = res_write res "Hello, World" in
    res_end res

let main () =
    let s = server_create handle in
    server_start s 8080;
    log "Listening to localhost:8080"