use io "../lib/core/io"

val make_adder : Int -> Int -> Int
let make_adder x =
  let adder y = x + y in
  adder

val main : a -> Unit
let main _ =
  let add1 = make_adder 1 in
  let res = add1 41 in
  println (to_string res)