use "../lib/core/io".

make_adder : Int -> Int -> Int.
make_adder x =
  let adder y = x + y in
  adder.

main : a -> Unit.
main _ =
  let add1 = make_adder 1 in
  let res = add1 41 in
  println (to_string res).