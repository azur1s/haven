use io "../lib/core/io"

val fib : Int -> Int
let fib x = case x
  | 0 -> x
  | 1 -> x
  else fib (x - 1) + fib (x - 2)

val main : a -> Unit
let main _ =
  let res = fib 10 in
  println (to_string res)