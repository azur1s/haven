let foo (f : a * b -> c * d) : unit = todo

let map (f : a -> b) (xs : a list) : b list = todo

let succ (x : int) : int = x + 1

let test : int list = map succ [1; 2; 3]

let main () : unit =
  let val : int = 42 in
  case val of
  | 42 -> println 0
  \ println val