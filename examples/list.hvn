use
  io   "../lib/core/io"
  list "../lib/core/list"

val xs : [Int]
let xs = [1, 2, 3, 4, 5]

let double x = x * 2

val main : 'a -> Unit
let main _ =
  print "Reversed: ";
  println (to_string (rev xs));
  print "Map: ";
  println (to_string (map double xs))