let main () =
    let rec fib (n : int) : int =
        if n == 0 || n == 1
        then n else fib (n - 1) + fib (n - 2)
    in
    let id = fun a -> a in
    let result : int = id fib 5 in
    __external__ "console.log" result