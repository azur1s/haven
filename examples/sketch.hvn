use
  io   "../lib/core/io",
  list "../lib/core/list".

xs : [Int].
xs = [1, 2, 3, 4, 5].

double x = x * 2.

main : a -> Unit.
main _ =
  print "Reversed: ";
  println (to_string (rev xs));
  print "Map: ";
  println (to_string (map double xs)).