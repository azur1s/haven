use std/io
use std/list
use std/basehttp
use std/num

type A = int * int

let foo (a: A) = a

let bar (a: A) = foo a

let foo (a: A) = (0, 0)

let main () =
    log (foo (1, 2));
    log (bar (1, 2));

    ()